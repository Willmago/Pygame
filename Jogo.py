import pygame
from config import *
from telas import init_screen, game_screen, map_init

# while state != QUIT:

#     clock.tick(FPS)
#     if state == INIT:
#         state = init_screen(window)
#     elif state == RUNNING:
#         state = game_screen(window)

# pygame.quit()

map_init(window, MAP)

